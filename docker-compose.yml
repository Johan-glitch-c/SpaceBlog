services:
  django:
    build: .
    container_name: blogspace

    ports:
      - "8000:8000"

    volumes:
      - .:/app
      - ./media:/app/media

    command: python manage.py runserver 0.0.0.0:8000